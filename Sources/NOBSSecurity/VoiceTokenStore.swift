/// NOBSSecurity — VoiceTokenStore
///
/// `VoiceTokenStore` was intentionally placed in the `NOBSVoice` module so
/// that it can reference `VoiceCredentials` without creating a circular
/// dependency between `NOBSSecurity` and `NOBSVoice`.
///
/// See `Sources/NOBSVoice/VoiceTokenStore.swift`.
